[package]
name = "rlox"
version = "0.1.0"
edition = "2024"

[dependencies]

[features]
debug_print_code = []
debug_trace_execution = []

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = true

[build]
rustflags = ["-C", "target-cpu=native"]
